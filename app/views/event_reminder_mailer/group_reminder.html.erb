<!DOCTYPE html>
<html>
<head>
  <style>
    body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; background: #f0f4f8; margin: 0; padding: 0; }
    .wrapper { padding: 40px 20px; }
    .container { max-width: 600px; margin: 0 auto; background: white; border-radius: 12px; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.08); }
    .header { background: linear-gradient(135deg, #047857, #059669); color: white; padding: 40px 30px; text-align: center; }
    .header .countdown { font-size: 40px; font-weight: bold; letter-spacing: -0.5px; margin: 0 0 4px; }
    .header p { margin: 0; font-size: 16px; opacity: 0.9; }
    .content { padding: 32px 30px; }
    .greeting { font-size: 16px; margin-bottom: 16px; }
    .message { font-size: 15px; color: #555; margin-bottom: 24px; line-height: 1.7; }
    .section-title { font-size: 13px; font-weight: bold; text-transform: uppercase; letter-spacing: 1px; color: #059669; margin-bottom: 12px; }
    .ticket-info { background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 20px; margin-bottom: 24px; }
    .ticket-info table { width: 100%; border-collapse: collapse; }
    .ticket-info td { padding: 8px 0; font-size: 14px; vertical-align: top; }
    .ticket-info td:first-child { font-weight: bold; color: #374151; width: 130px; }
    .ticket-info td:last-child { color: #555; }
    @media only screen and (max-width: 480px) {
      .ticket-info td { display: block !important; width: 100% !important; }
      .ticket-info td:first-child { padding-bottom: 0 !important; }
      .ticket-info td:last-child { padding-top: 2px !important; padding-bottom: 14px !important; }
    }
    .qr-note { font-size: 14px; color: #666; margin: 0 0 16px; }
    .attendee-card { border: 1px solid #e2e8f0; border-radius: 8px; padding: 20px; margin-bottom: 16px; text-align: center; }
    .attendee-card .attendee-name { font-size: 15px; font-weight: bold; color: #1e293b; margin: 0 0 2px; }
    .attendee-card .attendee-index { font-size: 12px; color: #94a3b8; margin: 0 0 16px; text-transform: uppercase; letter-spacing: 0.5px; }
    .attendee-card img { border: 4px solid white; box-shadow: 0 2px 8px rgba(0,0,0,0.12); border-radius: 4px; }
    .closing { font-size: 15px; color: #555; line-height: 1.7; }
    .footer { background: #f8fafc; border-top: 1px solid #e2e8f0; text-align: center; padding: 20px 30px; font-size: 12px; color: #999; }
  </style>
</head>
<body>
  <% ticket_types_uniform = @tickets.map(&:ticket_type_id).uniq.one? %>
  <div class="wrapper">
    <div class="container">
      <div class="header">
        <p class="countdown"><%= @days_until %> day<%= @days_until > 1 ? 's' : '' %> to go</p>
        <p><%= @days_until == 1 ? "See you tomorrow!" : "Event Reminder" %></p>
      </div>

      <div class="content">
        <p class="greeting">Hi <strong><%= @tickets.first.attendee_name %></strong>,</p>

        <p class="message">
          <strong><%= @event.title %></strong> is coming up <%= @days_until == 1 ? 'tomorrow' : "in #{@days_until} days" %>.
          Here's a reminder of your <%= @tickets.size %> tickets — have each QR code ready for check-in.
        </p>

        <div class="section-title">Event Details</div>
        <div class="ticket-info">
          <table>
            <tr>
              <td>Event:</td>
              <td><%= @event.title %></td>
            </tr>
            <tr>
              <td>Date:</td>
              <td><%= @event.start_date.strftime("%B %d, %Y") %></td>
            </tr>
            <% if @event.event_locations.any? %>
            <tr>
              <td>Venue:</td>
              <td><%= @event.event_locations.first.name %></td>
            </tr>
            <% end %>
            <% if ticket_types_uniform %>
            <tr>
              <td>Ticket Type:</td>
              <td><%= @tickets.first.ticket_type.name %></td>
            </tr>
            <% end %>
            <tr>
              <td>Tickets:</td>
              <td><%= @tickets.size %></td>
            </tr>
          </table>
        </div>

        <div class="section-title">Your Entry QR Codes</div>
        <p class="qr-note">Please present the relevant QR code at the registration counter for each attendee.</p>

        <% @tickets.each_with_index do |ticket, index| %>
          <div class="attendee-card">
            <p class="attendee-name"><%= ticket.attendee_name %></p>
            <p class="attendee-index">
              Ticket <%= index + 1 %> of <%= @tickets.size %>
              <% unless ticket_types_uniform %>&middot; <%= ticket.ticket_type.name %><% end %>
            </p>
            <%= image_tag attachments["qr_code_#{index}.png"].url, alt: "QR Code", width: 160, height: 160 %>
          </div>
        <% end %>

        <p class="closing">We look forward to seeing you there!</p>
      </div>

      <div class="footer">
        <p>This is an automated reminder from EventzFlow. Please do not reply to this email.</p>
      </div>
    </div>
  </div>
</body>
</html>
